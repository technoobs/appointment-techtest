import { ErrorlightDirective } from './errorlight.directive';

describe('ErrorlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorlightDirective();
    expect(directive).toBeTruthy();
  });
});
